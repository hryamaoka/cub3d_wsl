# 実装したボーナス機能

## マウスによる視点回転
マウスを左右に動かすと視点が回転します。

## 開閉するドア
- マップ内の `D` をドアとして認識します。
- ドアの前で `E` キーを押すと、開閉（`D` ↔ `O`）できます。

## アニメーションスプライト
- マップ内の `2` をスプライトとして表示します。
- 緑と赤の球体が点滅（アニメーション）します。
